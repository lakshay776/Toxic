# Dataset 

This directory contains the core Jigsaw Toxic Comment Classification Challenge dataset.

Because this repository is cloned directly in Google Colab during the initial setup cell, the dataset is automatically pulled into the environment alongside the code. No manual Kaggle API configuration is required!

**Files included:**
- `train.csv` (159,571 samples)
- `test.csv`
- `test_labels.csv`
